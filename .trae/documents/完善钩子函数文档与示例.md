## 目标
- 在 `algorithm_dev_tutorial.md` 新增并强化“钩子函数”章节，系统阐明每个钩子的职责、调用时机、参数语义与最佳实践。
- 在示例代码中补充钩子最小示例片段，体现资源加载/释放、步骤边界处理与诊断日志输出的规范用法。
- 为所有钩子提供精确的代码行号引用，保证三方团队可快速定位实现。

## 文档更新内容
- 钩子总览：`setup/teardown/on_step_start/on_step_finish/reset` 的作用域与典型用途。
- 调用顺序与差异：平台“步骤从1开始”与 Dev Runner `run/validate` 的调用序区别及兼容指导。
- 入参与返回：参数含义、返回值要求（统一返回 `None`）、异常处理建议与结构化日志。
- 资源生命周期：模型/缓存/句柄在 `setup/teardown/reset` 中的管理规范。
- 诊断与日志：在钩子中使用 `Diagnostics` 与 `StructuredLogger` 的推荐模式。
- 最佳实践：幂等性、轻量化、失败回收、性能采样、边界条件（步骤切换、PID 切换）。

## 示例增强
- 在 `algorithm-example/algorithm_example/main.py` 增加钩子最小实现片段（不改变现有行为）：
  - `setup`：标注模型版本与预热；
  - `on_step_start`：记录步骤上下文，重置计时；
  - `on_step_finish`：输出该步骤诊断指标；
  - `teardown`：释放资源；
  - `reset`：清空会话状态（演示）。

## 交叉引用
- 明确列出钩子定义位置与 CLI 调用位置：
  - 定义：`procvision_algorithm_sdk/base.py:17-30`
  - validate 调用：`procvision_algorithm_sdk/cli.py:78-80, 94-96, 117-124`
  - run 调用：`procvision_algorithm_sdk/cli.py:193-210`

## 验证与交付
- 运行现有测试确保无破坏：`tests/test_cli.py`、`tests/test_base_algo.py`。
- 仅文档与示例注释级增强，不引入新依赖。
- 获得确认后，执行文档更新与示例增强，并回传具体变更列表与定位引用。

请确认上述计划，确认后我将提交对应文档增强与示例补充。